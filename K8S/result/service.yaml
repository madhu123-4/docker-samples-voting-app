apiVersion: v1
kind: Service

metadata:
  name: result
  namespace: voting

spec:
  selector:
    app.kubernetes.io/name: result
    app.kubernetes.io/instance: result

  ports:
  - protocol: TCP
    port: 8081
    targetPort: 80